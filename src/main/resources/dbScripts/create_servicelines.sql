--DROP TABLE service_lines IF EXISTS;

CREATE TABLE service_lines (
  id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 100, INCREMENT BY 1) PRIMARY KEY,
  sl_code VARCHAR(10),
  sl_name VARCHAR(50)
);